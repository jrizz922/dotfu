# View what Process is Accessing a File (MacOS)

See what process is accessing a file in Mac OS X

`$ sudo fs_usage | grep [path_to_file]`

View current usage of file

`$ sudo lsof | grep [path_to_file]`

#macos/dotfiles	